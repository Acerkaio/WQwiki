把这些转为markdown源码给我：
# 鸽巢原理
```
基本内容：
	若 x1+x2+....+xn = m
	则 不可能 任意 xi <= floor((m-1)/n)
	也 不可能 任意 xi >= ceil(m/n)	
以下计该值为mid

适用范围：
	所有加和形式的式子都可考虑
具体处理：
	可以根据和mid的大小关系分类讨论
	核心是<=mid的数不可以组成一组

例题：
UOJ 元旦激光炮
ARC148E ≥ K
清澄 K大巨头
```